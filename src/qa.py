"""Day 2: Grounded QA over an extracted document.

Answers ad-hoc questions (e.g. "what's the total payable?") grounded in the
OCR'd text of a single document, rather than the structured extraction.
"""

from __future__ import annotations


def answer_question(ocr_text: str, question: str) -> str:
    # TODO(day2): build a grounded prompt (OCR text + question) and call
    # the configured model.
    raise NotImplementedError
